### 🚀 Run Project

1. Run 'Server.py'
2. Run 'chat.py'
3. Commands:
   - `auth`
     - Syntax: `auth {username} {password}`
   - `send`
     - Syntax: `send {username_destination} {message}`
   - `inbox`
     - Syntax: `inbox`